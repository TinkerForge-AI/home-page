---
import Layout from '../components/Layout.astro';
---

<Layout 
  title="Contact Us - TinkerForge AI"
  description="Get in touch with TinkerForge AI for partnerships, research collaboration, hardware contributions, consulting, media inquiries, and more."
>
  <main class="min-h-screen bg-gradient-to-br from-surface-dark via-surface to-surface-dark">
    <!-- Hero Section -->
    <section class="relative py-24">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <div class="animate-fade-in">
          <h1 class="text-4xl md:text-6xl font-bold mb-6 bg-gradient-to-r from-white via-primary-200 to-accent-300 bg-clip-text text-transparent">
            Get In Touch
          </h1>
          <p class="text-xl md:text-2xl text-gray-300 mb-8 leading-relaxed">
            We welcome partnerships, research collaboration, hardware contributions, and inquiries from the community
          </p>
          <div class="w-24 h-1 bg-gradient-to-r from-primary-500 to-accent-500 mx-auto rounded-full"></div>
        </div>
      </div>
    </section>

    <!-- Contact Options Section -->
    <section class="py-16">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="glass rounded-2xl p-8 mb-16 animate-on-scroll">
          <h2 class="text-2xl font-semibold mb-6 text-center">Why Contact Us?</h2>
          <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            <div class="text-center">
              <div class="w-16 h-16 bg-primary-600 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">Research Collaboration</h3>
              <p class="text-gray-400 text-sm">Partner with us on AI safety research projects</p>
            </div>
            <div class="text-center">
              <div class="w-16 h-16 bg-accent-600 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">Hardware Contribution</h3>
              <p class="text-gray-400 text-sm">Donate computing resources to accelerate our research</p>
            </div>
            <div class="text-center">
              <div class="w-16 h-16 bg-primary-600 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2-2v2m8 0V6a2 2 0 012 2v6a2 2 0 01-2 2H8a2 2 0 01-2-2V8a2 2 0 012-2V6" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">Contract Work</h3>
              <p class="text-gray-400 text-sm">Consulting services and custom AI safety solutions</p>
            </div>
            <div class="text-center">
              <div class="w-16 h-16 bg-accent-600 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9.5a2.5 2.5 0 00-2.5-2.5H15" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">Media & Press</h3>
              <p class="text-gray-400 text-sm">Interviews, press releases, and media inquiries</p>
            </div>
            <div class="text-center">
              <div class="w-16 h-16 bg-primary-600 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">Speaking Events</h3>
              <p class="text-gray-400 text-sm">Conference talks and educational presentations</p>
            </div>
            <div class="text-center">
              <div class="w-16 h-16 bg-accent-600 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">Partnership</h3>
              <p class="text-gray-400 text-sm">Strategic partnerships and collaboration opportunities</p>
            </div>
          </div>
          <div class="text-center mt-8 text-gray-400">
            <p>We typically respond to all inquiries within <span class="text-primary-400 font-semibold">1-2 business days</span>. All inquiries are welcome!</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Form Section -->
    <section class="py-16">
      <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="glass rounded-2xl p-8 animate-on-scroll">
          <h2 class="text-2xl font-semibold mb-8 text-center">Send Us a Message</h2>
          
          <form id="contact-form" class="space-y-6">
            <!-- Name Field -->
            <div>
              <label for="name" class="block text-sm font-medium text-gray-300 mb-2">
                Name <span class="text-red-400">*</span>
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 bg-surface-light border border-surface-border rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 text-white placeholder-gray-400 transition-colors duration-200"
                placeholder="Your full name"
              />
            </div>

            <!-- Organization Field -->
            <div>
              <label for="organization" class="block text-sm font-medium text-gray-300 mb-2">
                Organization
              </label>
              <input
                type="text"
                id="organization"
                name="organization"
                class="w-full px-4 py-3 bg-surface-light border border-surface-border rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 text-white placeholder-gray-400 transition-colors duration-200"
                placeholder="Company, university, or organization (optional)"
              />
            </div>

            <!-- Contact Type Dropdown -->
            <div>
              <label for="contact-type" class="block text-sm font-medium text-gray-300 mb-2">
                Contact Type <span class="text-red-400">*</span>
              </label>
              <select
                id="contact-type"
                name="contact-type"
                required
                class="w-full px-4 py-3 bg-surface-light border border-surface-border rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 text-white transition-colors duration-200"
              >
                <option value="general-inquiry" selected>General Inquiry</option>
                <option value="contract-work">Contract Work / Consulting</option>
                <option value="hardware-contribution">Hardware Contribution</option>
                <option value="research-collaboration">Research Collaboration</option>
                <option value="suggested-research">Suggested Research Topic</option>
                <option value="media-press">Media/Press Inquiry</option>
                <option value="speaking-engagement">Speaking Engagement</option>
                <option value="partnership">Partnership Opportunity</option>
                <option value="other">Other</option>
              </select>
            </div>

            <!-- Contact Information -->
            <div class="grid md:grid-cols-2 gap-4">
              <div>
                <label for="email" class="block text-sm font-medium text-gray-300 mb-2">
                  Email Address <span class="text-red-400">*</span>
                </label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  class="w-full px-4 py-3 bg-surface-light border border-surface-border rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 text-white placeholder-gray-400 transition-colors duration-200"
                  placeholder="your.email@example.com"
                />
              </div>
              <div>
                <label for="phone" class="block text-sm font-medium text-gray-300 mb-2">
                  Phone Number
                </label>
                <input
                  type="tel"
                  id="phone"
                  name="phone"
                  class="w-full px-4 py-3 bg-surface-light border border-surface-border rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 text-white placeholder-gray-400 transition-colors duration-200"
                  placeholder="+1 (555) 123-4567"
                />
              </div>
            </div>
            
            <!-- Contact Method Note -->
            <div class="text-sm text-gray-400 bg-surface-dark p-3 rounded-lg">
              <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              Please provide at least one contact method (email or phone)
            </div>

            <!-- Message Field -->
            <div>
              <label for="message" class="block text-sm font-medium text-gray-300 mb-2">
                Message <span class="text-red-400">*</span>
              </label>
              <textarea
                id="message"
                name="message"
                rows="6"
                required
                class="w-full px-4 py-3 bg-surface-light border border-surface-border rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 text-white placeholder-gray-400 transition-colors duration-200 resize-vertical"
                placeholder="Tell us about your inquiry, project, or how we can help you..."
              ></textarea>
            </div>

            <!-- Submit Button -->
            <div class="text-center">
              <button
                type="submit"
                class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-accent-600 to-primary-600 hover:from-accent-500 hover:to-primary-500 text-white rounded-lg font-medium transition-all duration-200 shadow-lg hover:shadow-xl hover:scale-105 glow-border"
              >
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
                Send Message
              </button>
            </div>

            <!-- Form Status Messages -->
            <div id="form-status" class="hidden p-4 rounded-lg"></div>
          </form>
        </div>
      </div>
    </section>

    <!-- Alternative Contact Methods -->
    <section class="py-16">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="glass rounded-2xl p-8 animate-on-scroll">
          <h2 class="text-2xl font-semibold mb-8 text-center">Alternative Contact Methods</h2>
          <div class="grid md:grid-cols-3 gap-8">
            <!-- Direct Email -->
            <div class="text-center">
              <div class="w-16 h-16 bg-gradient-to-br from-blue-600 to-purple-600 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">Direct Email</h3>
              <p class="text-gray-400 mb-4">For immediate communication</p>
              <a 
                href="mailto:tinkerforgeai@gmail.com" 
                class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-lg text-sm font-medium hover:shadow-lg transition-all duration-200 hover:scale-105"
              >
                tinkerforgeai@gmail.com
              </a>
            </div>

            <!-- GitHub -->
            <div class="text-center">
              <div class="w-16 h-16 bg-gradient-to-br from-gray-700 to-gray-900 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">GitHub</h3>
              <p class="text-gray-400 mb-4">Technical discussions and open source</p>
              <a 
                href="https://github.com/TinkerForge-AI" 
                class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-gray-700 to-gray-900 text-white rounded-lg text-sm font-medium hover:shadow-lg transition-all duration-200 hover:scale-105"
              >
                @TinkerForge-AI
              </a>
            </div>

            <!-- Secure Communication -->
            <div class="text-center">
              <div class="w-16 h-16 bg-gradient-to-br from-green-600 to-green-800 rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                </svg>
              </div>
              <h3 class="text-lg font-semibold mb-2">Secure Communication</h3>
              <p class="text-gray-400 mb-4">For sensitive or confidential matters</p>
              <button 
                onclick="showPGP()"
                class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-green-600 to-green-800 text-white rounded-lg text-sm font-medium hover:shadow-lg transition-all duration-200 hover:scale-105"
              >
                View PGP Key
              </button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- PGP Key Modal (hidden by default) -->
    <div id="pgp-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 hidden z-50">
      <div class="glass rounded-2xl p-8 max-w-2xl w-full max-h-96 overflow-y-auto">
        <div class="flex justify-between items-center mb-4">
          <h3 class="text-xl font-semibold">PGP Public Key</h3>
          <button onclick="hidePGP()" class="text-gray-400 hover:text-white">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
        <div class="bg-surface-dark p-4 rounded-lg">
          <code class="text-xs text-gray-300 whitespace-pre-wrap break-all font-mono">
-----BEGIN PGP PUBLIC KEY BLOCK-----

mQENBGXExample123456789ABCDEF...
(PGP key would be displayed here)
...END PGP PUBLIC KEY BLOCK-----
          </code>
        </div>
        <div class="mt-4 text-sm text-gray-400">
          <p>Download our PGP key for encrypted communication. Use this for sensitive research discussions or security-related inquiries.</p>
        </div>
      </div>
    </div>
  </main>
</Layout>

<script>
  // Form validation and submission
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('contact-form');
    const formStatus = document.getElementById('form-status');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      // Get form data
      const formData = new FormData(form);
      const data = Object.fromEntries(formData.entries());

      // Validate required fields
      const errors = [];
      
      if (!data.name.trim()) {
        errors.push('Name is required');
      }
      
      if (!data['contact-type']) {
        errors.push('Contact type is required');
      }
      
      if (!data.email.trim() && !data.phone.trim()) {
        errors.push('Please provide either an email address or phone number');
      }
      
      if (data.email && !isValidEmail(data.email)) {
        errors.push('Please enter a valid email address');
      }
      
      if (!data.message.trim()) {
        errors.push('Message is required');
      }

      // Show errors if any
      if (errors.length > 0) {
        showFormStatus('error', 'Please fix the following errors:<br>' + errors.map(e => '• ' + e).join('<br>'));
        return;
      }

      // Show loading state
      const submitButton = form.querySelector('button[type="submit"]');
      const originalText = submitButton.innerHTML;
      submitButton.innerHTML = '<svg class="animate-spin w-5 h-5 mr-2" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="m4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>Sending...';
      submitButton.disabled = true;

      try {
        // Simulate form submission (replace with actual endpoint)
        await simulateFormSubmission(data);
        
        showFormStatus('success', 'Thank you for your message! We\'ll get back to you within 1-2 business days.');
        form.reset();
      } catch (error) {
        showFormStatus('error', 'Sorry, there was an error sending your message. Please try again or email us directly.');
      }

      // Restore button
      submitButton.innerHTML = originalText;
      submitButton.disabled = false;
    });

    function isValidEmail(email) {
      return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
    }

    function showFormStatus(type, message) {
      formStatus.className = `p-4 rounded-lg ${type === 'success' ? 'bg-green-900 text-green-300 border border-green-700' : 'bg-red-900 text-red-300 border border-red-700'}`;
      formStatus.innerHTML = message;
      formStatus.classList.remove('hidden');
      formStatus.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    }

    async function simulateFormSubmission(data) {
      // Replace this with actual form submission logic
      // For now, just simulate a delay
      return new Promise((resolve) => {
        setTimeout(resolve, 1500);
      });
    }
  });

  // PGP modal functions
  function showPGP() {
    document.getElementById('pgp-modal').classList.remove('hidden');
    document.body.style.overflow = 'hidden';
  }

  function hidePGP() {
    document.getElementById('pgp-modal').classList.add('hidden');
    document.body.style.overflow = 'auto';
  }

  // Close modal on escape key
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      hidePGP();
    }
  });

  // Close modal on backdrop click
  document.getElementById('pgp-modal').addEventListener('click', (e) => {
    if (e.target.id === 'pgp-modal') {
      hidePGP();
    }
  });

  // Add scroll animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('animate-slide-up');
      }
    });
  }, observerOptions);
  
  document.querySelectorAll('.animate-on-scroll').forEach(el => {
    observer.observe(el);
  });
</script>